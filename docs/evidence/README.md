# Acceptance evidence

Create one directory per traceability ID (`AC-01` through `AC-10`). Store only redacted reports, hashes, screenshots from the deterministic fixture, and links to approved external evidence systems. Never commit public-surface page bodies, credentials, tokens, or sensitive artifacts to Git.
