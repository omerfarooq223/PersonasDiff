# Development environment

Local Docker Compose is the source of truth. Copy `.env.example` to `.env` only for host-run commands. Development credentials in the example files are intentionally local-only and must never be reused outside a developer machine.
