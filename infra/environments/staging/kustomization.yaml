apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
# Add ingress.example.yaml only after replacing its host, TLS, and ingress-class settings.
