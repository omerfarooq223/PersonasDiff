{
  "name": "@ai-parallel-web/browser-spike",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "spike": "tsx src/run.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "playwright": "^1.50.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0"
  }
}
