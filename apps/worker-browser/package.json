{
  "name": "@ai-parallel-web/worker-browser",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-parallel-web/contracts": "*",
    "@ai-parallel-web/domain": "*",
    "playwright": "^1.50.0"
  }
}
