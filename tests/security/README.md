# Security tests

Day 1 security coverage includes allowlist tests and the initial threat model. Add SSRF, redirect escape, private-range, stored-XSS, artifact authorization, secret-redaction, and deletion cases as their controls are implemented.
